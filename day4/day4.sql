1. select sum(salary) as total_salary from employees;
2. select avg(salary) as average_salary from employees;
3. select min(salary) as min_salary, max(salary) as max_salary from employees;
4. select count(*) as total_employees from employees;
